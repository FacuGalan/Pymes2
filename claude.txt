Necesito que me armes un plan de trabajo para crear un modulo para generar lo que en mi sistema manager son las ventas que aplican como compras a mis clientes franquicias. 
Yo tengo en mi base de datos Mysql una tabla que se llama ma_puntos, que tiene la siguiente estructura:
CREATE TABLE `ma_puntos` (
  `id` int(3) NOT NULL AUTO_INCREMENT,
  `punto` varchar(6) NOT NULL,
  `detalle` varchar(50) NOT NULL,
  `esmanager` tinyint(1) NOT NULL DEFAULT '0',
  PRIMARY KEY (`id`)
) ENGINE=;
Luego cada cliente tabla del sistema de cada cliente tiene un prefijo con el numero que coincide con el campo punto. Por ejemplo
CREATE TABLE `ge_000014ventas_encab` (
  `TICOMP` varchar(2) DEFAULT NULL,
  `LETRA` varchar(1) DEFAULT NULL,
  `NUMCOMP` varchar(13) DEFAULT NULL,
  `CODCLI` int(6) DEFAULT NULL,
  `FECHA` date DEFAULT NULL,
  `NETO` decimal(10,2) NOT NULL DEFAULT '0.00',
  `IVA` decimal(10,2) NOT NULL DEFAULT '0.00',
  `IIBB` decimal(10,2) NOT NULL DEFAULT '0.00',
  `SOBRETASA` decimal(10,2) DEFAULT NULL,
  `IMPORTE` decimal(10,2) NOT NULL DEFAULT '0.00',
  `PERCEP` decimal(5,2) NOT NULL DEFAULT '0.00',
  `TIPOPAG` varchar(1) DEFAULT NULL,
  `OBSERVA` varchar(255) DEFAULT NULL,
  `cae` varchar(20) DEFAULT NULL,
  `fecvto` date DEFAULT NULL,
  `tipfor` varchar(2) DEFAULT NULL,
  `IMPINT` decimal(10,2) NOT NULL DEFAULT '0.00',
  `USUARIO` varchar(15) DEFAULT NULL,
  `VENDEDOR` varchar(50) DEFAULT NULL,
  `FECMOD` date DEFAULT NULL,
  `IP` varchar(40) DEFAULT NULL,
  `NOMBRE` varchar(50) DEFAULT NULL,
  `CUIT` varchar(13) DEFAULT NULL,
  `DNI` int(8) DEFAULT NULL,
  `DIRECCION` varchar(50) DEFAULT NULL,
  `LOCALIDAD` varchar(50) DEFAULT NULL,
  `CONIVA` int(1) DEFAULT NULL,
  `acopio` tinyint(1) DEFAULT '0',
  `condven` int(3) DEFAULT NULL,
  `formapag` int(3) DEFAULT NULL,
  `codbarra` int(10) NOT NULL AUTO_INCREMENT,
  `fechaasig` date DEFAULT NULL,
  `codrep` int(4) NOT NULL DEFAULT '0',
  `cartaporte` int(10) DEFAULT '0',
  `hora` varchar(8) NOT NULL DEFAULT '00,00,00',
  `endolares` tinyint(1) NOT NULL DEFAULT '0',
  `cotiza` decimal(12,2) NOT NULL DEFAULT '0.00',
  `puntos` int(8) DEFAULT '0',
  `puntosacu` int(8) DEFAULT '0',
  KEY `CODCLI` (`CODCLI`),
  KEY `FECHA` (`FECHA`),
  KEY `codbarra` (`codbarra`),
  KEY `idx_ventas_encab` (`TICOMP`,`LETRA`,`NUMCOMP`,`FECHA`),
  CONSTRAINT `ge_000014ventas_encab_ibfk_1` FOREIGN KEY (`CODCLI`) REFERENCES `ge_000014clientes` (`CODIGO`)
) ENGINE=InnoDB; Corresponde a mi cliente Nro 14, y una igual hay para el cliente 15, pero se llama ge_000015ventas_encab.
Las ventas que el cliente 14 que es el principal del sistema, le hace a cualquier cliente que no sea una franquicia, descuentan de stock los productos vendidos, generan el registro de ventas, de ventas_cuota que es para la cuenta corriente, imputan el pago si es que pagan, etc etc. Todo eso esta en el archivo prg adjunto que pongo (ventas.prg). Pero para el caso de las franquicias, además este proceso debería generar un proceso inverso en compras. A estos, debería generarle la carga del comprobante de compra automáticamente en su sistema, subir el stock de los productos comprados, y dejar la factura en cuenta corriente para el futuro pago.
Adjunto el modulo compras (compras.prg) para su análisis. 
Necesito una estrategia para este proceso. Solo quiero agregar una tabla que diga, para cada cliente franquicia, cual es numero de prefijo asignado y con que nro de proveedor está referenciado el proveedor del Manager (llamemos ma_config a esta tabla). Fijate si necesitas más datos.
Detallo también las tablas intervinientes:
CREATE TABLE `ge_000014ventas_cuota` (
  `TIPO` varchar(2) DEFAULT NULL,
  `LETRA` varchar(1) DEFAULT NULL,
  `NUMERO` varchar(13) DEFAULT NULL,
  `CUOTA` int(2) DEFAULT NULL,
  `CANTCUO` int(2) DEFAULT NULL,
  `FECHA` date DEFAULT NULL,
  `CLIENTE` int(6) DEFAULT NULL,
  `IMPORTE` decimal(10,2) DEFAULT NULL,
  `SALDO` decimal(10,2) DEFAULT NULL,
  `ESTADO` varchar(1) DEFAULT NULL,
  `NETO` decimal(10,2) NOT NULL DEFAULT '0.00',
  `FECVTO` date DEFAULT NULL,
  `USUARIO` varchar(15) DEFAULT NULL,
  `FECMOD` date DEFAULT NULL,
  `IP` varchar(40) DEFAULT NULL,
  `VENDEDOR` varchar(50) DEFAULT NULL,
  `NOMBRE` varchar(50) DEFAULT NULL,
  `CUIT` varchar(13) DEFAULT NULL,
  `DNI` int(8) DEFAULT NULL,
  `DIRECCION` varchar(50) DEFAULT NULL,
  `LOCALIDAD` varchar(50) DEFAULT NULL,
  `saldodolar` decimal(12,4) NOT NULL DEFAULT '0.0000',
  KEY `CLIENTE` (`CLIENTE`),
  CONSTRAINT `ge_000014ventas_cuota_ibfk_1` FOREIGN KEY (`CLIENTE`) REFERENCES `ge_000014clientes` (`CODIGO`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8;

CREATE TABLE `ge_000014ventas_det` (
  `ID` int(10) NOT NULL AUTO_INCREMENT,
  `CODART` bigint(13) NOT NULL,
  `DETART` varchar(200) NOT NULL,
  `CANTIDAD` decimal(9,3) NOT NULL DEFAULT '0.000',
  `PUNIT` decimal(10,2) NOT NULL DEFAULT '0.00',
  `FECHA` date NOT NULL,
  `CODCLI` int(10) NOT NULL DEFAULT '0',
  `NROFAC` varchar(16) NOT NULL,
  `IMPORTE` decimal(10,2) NOT NULL DEFAULT '0.00',
  `NETO` decimal(10,2) NOT NULL DEFAULT '0.00',
  `IVA` decimal(10,2) NOT NULL,
  `CODIVA` int(3) NOT NULL,
  `NETON` decimal(10,2) DEFAULT '0.00',
  `DESCU` decimal(10,2) DEFAULT '0.00',
  `PCOSTO` decimal(10,2) DEFAULT '0.00',
  `BULTOS` decimal(7,2) DEFAULT '0.00',
  `IMPINT` decimal(10,2) NOT NULL DEFAULT '0.00',
  `descup` decimal(5,2) NOT NULL DEFAULT '0.00',
  PRIMARY KEY (`ID`,`CODART`,`CODCLI`,`NROFAC`),
  KEY `CODART` (`CODART`),
  KEY `FECHA` (`FECHA`),
  KEY `NROFAC` (`NROFAC`)
) ENGINE=InnoDB;

CREATE TABLE `ge_000014ventivadet` (
  `TIPOCOMP` varchar(2) NOT NULL,
  `LETRA` varchar(1) NOT NULL,
  `NUMFAC` varchar(13) NOT NULL,
  `CODIVA` int(2) NOT NULL,
  `NETO` decimal(10,2) DEFAULT NULL,
  `IVA` decimal(10,2) DEFAULT NULL,
  PRIMARY KEY (`TIPOCOMP`,`LETRA`,`NUMFAC`,`CODIVA`),
  KEY `idx_ventivadet` (`TIPOCOMP`,`LETRA`,`NUMFAC`,`CODIVA`)
) ENGINE=InnoDB;

//Compras
CREATE TABLE `ge_000014compras` (
  `CODPRO` int(8) NOT NULL,
  `TIPOCOMP` varchar(2) NOT NULL,
  `LETRA` varchar(1) NOT NULL,
  `NUMFAC` varchar(13) NOT NULL,
  `FECFAC` date DEFAULT NULL,
  `FECVTO` date DEFAULT NULL,
  `FECING` date DEFAULT NULL,
  `NOGRABADO` decimal(12,2) NOT NULL DEFAULT '0.00',
  `NETO` decimal(12,2) NOT NULL DEFAULT '0.00',
  `IVA` decimal(12,2) NOT NULL DEFAULT '0.00',
  `IMPORTE` decimal(12,2) DEFAULT '0.00',
  `CODCUE` int(4) DEFAULT '1',
  `ESTADO` varchar(1) DEFAULT NULL,
  `OBSERVA` varchar(20) DEFAULT NULL,
  `SALDO` decimal(12,2) DEFAULT '0.00',
  `IMPUTAIVA` tinyint(1) NOT NULL DEFAULT '1',
  `USUARIO` varchar(15) DEFAULT '"MIGRA"',
  `NOMOSTRAR` tinyint(1) NOT NULL DEFAULT '0',
  `FECMOD` date DEFAULT NULL,
  `IP` varchar(40) DEFAULT NULL,
  `lnocomputaiva` tinyint(1) NOT NULL DEFAULT '0',
  PRIMARY KEY (`CODPRO`,`TIPOCOMP`,`LETRA`,`NUMFAC`),
  CONSTRAINT `ge_000014compras_ibfk_1` FOREIGN KEY (`CODPRO`) REFERENCES `ge_000014provee` (`CODIGO`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8;
CREATE TABLE `ge_000014compradet` (
  `CODPRO` int(8) NOT NULL,
  `TIPOCOMP` varchar(2) NOT NULL,
  `LETRA` varchar(1) NOT NULL,
  `NUMFAC` varchar(13) NOT NULL,
  `RENGLON` int(2) NOT NULL,
  `CODART` bigint(14) NOT NULL,
  `CANTIDAD` decimal(10,3) DEFAULT NULL,
  `PUNIT` decimal(10,2) DEFAULT NULL,
  `NETO` decimal(10,2) DEFAULT NULL,
  `IVA` decimal(10,2) DEFAULT NULL,
  `CODIVA` int(2) DEFAULT NULL,
  `desc1` decimal(5,2) NOT NULL DEFAULT '0.00',
  `desc2` decimal(5,2) NOT NULL DEFAULT '0.00',
  `desc3` decimal(5,2) NOT NULL DEFAULT '0.00',
  `desc4` decimal(5,2) NOT NULL DEFAULT '0.00',
  `desc5` decimal(5,2) NOT NULL DEFAULT '0.00',
  `siniva` tinyint(1) NOT NULL DEFAULT '0',
  PRIMARY KEY (`CODPRO`,`TIPOCOMP`,`LETRA`,`NUMFAC`,`RENGLON`,`CODART`),
  KEY `CODART` (`CODART`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8;
CREATE TABLE `ge_000014compivadet` (
  `CODPRO` int(8) NOT NULL,
  `TIPOCOMP` varchar(2) NOT NULL,
  `LETRA` varchar(1) NOT NULL,
  `NUMFAC` varchar(13) NOT NULL,
  `CODIVA` int(2) NOT NULL,
  `NETO` decimal(10,2) DEFAULT '0.00',
  `IVA` decimal(10,2) DEFAULT '0.00',
  PRIMARY KEY (`CODPRO`,`TIPOCOMP`,`LETRA`,`NUMFAC`,`CODIVA`)
) ENGINE=InnoDB DEFAULT;
CREATE TABLE `ge_000014clientes` (
  `CODIGO` int(8) NOT NULL AUTO_INCREMENT,
  `NOMBRE` varchar(50) NOT NULL,
  `DIRECCION` varchar(50) NOT NULL,
  `LOCALIDAD` varchar(30) NOT NULL,
  `TELEFONO` varchar(30) NOT NULL,
  `CUIT` varchar(13) NOT NULL,
  `CONIVA` int(2) NOT NULL,
  `VENDEDOR` int(2) NOT NULL,
  `DNI` int(9) NOT NULL,
  `SALDO` decimal(10,2) NOT NULL,
  `MAIL` varchar(50) NOT NULL,
  `CODPOS` varchar(8) DEFAULT NULL,
  `ZONA` varchar(6) DEFAULT NULL,
  `FECING` date DEFAULT NULL,
  `FECULT` date DEFAULT NULL,
  `FECNAC` date DEFAULT NULL,
  `DESCUENTO` decimal(5,2) DEFAULT '0.00',
  `LISPRE` int(1) DEFAULT NULL,
  `LIMITE` decimal(10,2) NOT NULL DEFAULT '0.00',
  `NUMTAR` varchar(16) DEFAULT NULL,
  `PUNTOS` int(5) NOT NULL DEFAULT '0',
  `IIBB` decimal(5,2) NOT NULL DEFAULT '0.00',
  `LISPREESP` int(3) NOT NULL DEFAULT '0',
  `USUARIO` varchar(15) DEFAULT NULL,
  `FECMOD` date DEFAULT NULL,
  `IP` varchar(40) DEFAULT NULL,
  `alias` varchar(15) DEFAULT NULL,
  `excluyepromo` tinyint(1) NOT NULL DEFAULT '0',
  PRIMARY KEY (`CODIGO`)
) ENGINE=InnoDB AUTO_INCREMENT=7 DEFAULT CHARSET=utf8;
CREATE TABLE `ge_000014provee` (
  `CODIGO` int(6) NOT NULL AUTO_INCREMENT,
  `NOMBRE` varchar(50) NOT NULL,
  `ALIAS` varchar(15) NOT NULL,
  `CUIT` varchar(13) DEFAULT NULL,
  `LOCALIDAD` varchar(50) DEFAULT NULL,
  `CODPOS` int(4) NOT NULL DEFAULT '0',
  `DIRECCION` varchar(60) DEFAULT NULL,
  `TELEFONO` varchar(60) DEFAULT NULL,
  `CONIVA` int(2) NOT NULL DEFAULT '0',
  `CODCUE` int(2) NOT NULL DEFAULT '0',
  `SALDO` decimal(10,2) NOT NULL DEFAULT '0.00',
  `RETENCIO` decimal(6,3) NOT NULL DEFAULT '0.000',
  `SALDOD` decimal(10,2) NOT NULL DEFAULT '0.00',
  `MAIL` varchar(60) NOT NULL,
  `CTABANCO` varchar(40) DEFAULT NULL,
  `OBSERVA` varchar(80) DEFAULT NULL,
  `RETIVA` decimal(5,2) NOT NULL DEFAULT '0.00',
  `GASTO` tinyint(1) NOT NULL DEFAULT '0',
  `EXGANA` tinyint(1) NOT NULL DEFAULT '0',
  `DIASVENCI` int(3) DEFAULT '0',
  `INACTIVO` tinyint(1) NOT NULL DEFAULT '0',
  PRIMARY KEY (`CODIGO`)
) ENGINE=InnoDB AUTO_INCREMENT=5 DEFAULT CHARSET=utf8;
